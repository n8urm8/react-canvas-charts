{
  "name": "react-charting-exploration",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter demo-app dev",
    "lint": "pnpm -r lint",
    "preview": "pnpm --filter demo-app preview"
  }
}
